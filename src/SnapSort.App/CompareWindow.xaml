<Window x:Class="SnapSort.App.CompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SnapSort" Icon="Assets/Branding/SnapSort.ico" Width="1180" Height="760" MinWidth="720" MinHeight="520" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}" Style="{StaticResource DialogWindowStyle}">
    <DockPanel TextElement.Foreground="{DynamicResource TextBrush}">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="360" Margin="14" Style="{StaticResource CardBorder}" Padding="10">
                            <Border.ContextMenu>
                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="Przenieś do kosza" Click="MovePhotoToTrash" />
                                </ContextMenu>
                            </Border.ContextMenu>
                            <StackPanel>
                                <Image Source="{Binding Thumbnail}" Height="460" Stretch="Uniform" />
                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" Margin="0,10,0,4" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding DisplayDate, StringFormat={}{0:dd.MM.yyyy HH:mm:ss}}" Foreground="{DynamicResource MutedBrush}" />
                                <TextBlock Foreground="{DynamicResource MutedBrush}" Margin="0,4,0,0"><Run Text="{Binding Width}" /><Run Text=" × " /><Run Text="{Binding Height}" /></TextBlock>
                                <TextBlock Text="{Binding SizeLabel}" Foreground="{DynamicResource MutedBrush}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
